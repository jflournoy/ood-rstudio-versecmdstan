# Find available port to run server on
readonly port=$(find_port ${host} 12000 14000  )

# Define a password and export it for RStudio authentication
password="$(create_passwd 16)"
export RSTUDIO_PASSWORD="${password}"
